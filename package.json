{
  "name": "alara-os",
  "version": "0.0.1",
  "private": true,
  "description": "Alara OS — Organizational Intelligence Platform (M0 Spine)",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "migrate": "node scripts/migrate.js",
    "migrate:down": "node scripts/migrate.js down"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "@types/uuid": "^9.0.8",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
