{
  "name": "alara-os",
  "version": "0.0.1",
  "private": true,
  "description": "Alara OS — Organizational Intelligence Platform (M0 Spine)",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "install:api": "npm --prefix apps/api ci",
    "test:api": "npm --prefix apps/api test",
    "build:api": "npm --prefix apps/api run build",
    "test:all": "npm run test --workspaces --if-present && npm run test:api",
    "build:all": "npm run build --workspaces && npm run build:api",
    "verify": "npm run test:all && npm run build:all",
    "migrate": "node scripts/migrate.js",
    "migrate:down": "node scripts/migrate.js down"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "@types/uuid": "^9.0.8",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
