# runtime
data/analytics.log
# alara os v0 sqlite store (local; use a persistent disk in prod)
data/ops.db
data/ops.db-wal
data/ops.db-shm
# python
__pycache__/
*.pyc
# os/editor
.DS_Store
# node (app is zero-dep; nothing should be installed, but just in case)
node_modules/
